namespace:
  'dplav1'
url: 
  'http://api.dp.la/v1'

parameters: 
  fields: &fields
    mode: field
    keywords: 
      [q,
      title,
      description,
      subject,
      dplaContributor,
      creator,
      type,
      publisher,
      format,
      rights,
      contributor,
      spatial,
      spatial.city,
      spatial.state,
      spatial.iso3166-2,
      spatial.coordinates,
      spatial.distance,
      isPartOf,
      temporal.before,
      temporal.after]
    sort_by: {sort_by: ''}
  
api:
  items: &items
    path: '/items?{0}'
    input:
      type: key_value
      params: *fields
      options: [page, page_size]
      syntax:
        field:
          bind: '='
          chain: '&'
        option:
          bind: '='
          chain: '&'
    output:
      default: json

  default: *items